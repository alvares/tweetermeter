!!!
%html{ html_attrs }
  %head
    %title= h yield(:title) || "TweeterMeter::CurseRating"
    
    - bundle do 
      = stylesheet_link_tag     :common
      = javascript_include_tag  :common
      = yield(:head)

    %body
      = yield
